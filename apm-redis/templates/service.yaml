apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-redis-svc
  labels:
    dep: {{ .Chart.Name }}-redis-svc
spec:
  type: NodePort
  ports:
    - port: 6379
      targetPort: redis-port
  selector:
    app: {{ .Chart.Name }}-redis
