apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ .Chart.Name }}-ingress
spec:
  rules:
  - host: 192.168.1.244
    http:
      paths:
      - path: /lensData
        backend:
          serviceName: {{ .Chart.Name }}-svc
          servicePort: 8080
  