apiVersion: v1
kind: ConfigMap
metadata: 
  name: {{ .Chart.Name }}-env-config
  namespace: {{ .Release.Namespace }}
data:
  env-config: |-
    var API_URL = {
      BASIC: {
        BASE_URL: '/ls/api',
        HOST:'',
        PORT: "",
        TEMPLATE: {
          url: '/template',
        },
        MODEL: { // object/getModels 获取实体类型列表
          url: '/model',
        },
        IMPORT_DATA: { // 导入数据
          url: '/dataset/import'
        },
        UPLOAD: {
          url: '/upload',
        }
      },
    }


    // 写死的case_id
    window.CASE_ID = 817845123;
        